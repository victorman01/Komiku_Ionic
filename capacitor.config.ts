import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'project_uas_hybrid',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
